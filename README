Description: all my config files for my home directory when I need to get set up on a new compter.  

Git Info: git@github.com:abelnation/homedir.git

= = = = = = = = = = = = 

Git config commands to run on a new machine:
> git config --global user.name "Abel Allison"
> git config --global user.email abel.allison@gmail.com


Git commands:

commit all local changes:
> git commit -a [-m 'commit message']

push to github:
> git push origin
OR
> git push git@github.com:abelnation/homedir.git master



